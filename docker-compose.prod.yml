version: "3.9"

services:
  backend:
    build: ./backend
    env_file:
      - .env
    environment:
      - ENV=production
    restart: unless-stopped
    expose:
      - "8000"

  frontend:
    build: ./frontend
    restart: unless-stopped
    expose:
      - "80"
